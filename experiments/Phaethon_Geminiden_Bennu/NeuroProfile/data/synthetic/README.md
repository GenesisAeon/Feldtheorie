# Synthetic NeuroProfile Data

- `synthetic_eeg_1khz.csv`: 1 kHz synthetic EEG slice (512 samples) combining 20 Hz + 40 Hz oscillations with light Gaussian noise.
- Use for smoke tests of β extraction, σΦ proxy, and resonance coupling.
