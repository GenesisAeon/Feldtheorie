# Legacy & History (V1–V3)

This shelf preserves earlier UTAC resonances so the v4.0 Mirror Machine stays
focused and reproducible:

- **FraktaltagebuchV2** and **RoadToV.3** now live here as canonical references;
  legacy symlinks in `seed/` keep older cross-links intact.
- **Seed note bundle:** early brainstorms, validation drafts, and Gemini session
  notes sit in `seed/notes/`, archived without deletion.
- **Data backups:** historical Aletheia CSV snapshots reside under
  `data/experimental/backups/` while the golden-master CSVs stay in
  `data/experimental/` for v4.0 fits.

If you need to recover a resonance path, start here, trace the logistic quartet
\((R, \Theta, \beta, \zeta(R))\), and mirror any reactivations back into the
current indices.
